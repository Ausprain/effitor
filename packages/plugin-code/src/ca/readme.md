这个一个demo，混合代码编辑与渲染，不使用 textarea；目前只是一个测试，产物中不包含此文件夹内代码

后续的构想时，将此方案作为额外的补充。
现在代码块的实现比较重，对于那些只渲染一行的代码，确实有些大材小用了。使用此方案作为补充，即额外增加一个【代码行】效应元素，只在渲染一行代码时使用。
不使用 textarea 方案，直接渲染 hightlighter 生成的 html 代码，然后使用【光标偏移量实时计算】的方式来确保每次编辑、渲染都能保持准确的光标位置。
