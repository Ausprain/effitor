// import './src/assets/font.css'

// import { Effitor, Et, EtHeading, EtParagraphElement, hotstring } from './src'

// const editor = new Effitor({
//   schemaInit: {
//     heading: EtHeading,
//   },
//   plugins: [
//     {
//       name: 'xxx',
//       effector: {
//         onMounted(ctx, signal) {
//           // ctx.hotstringManager.create('hello', (ctx, removeInsertedHotstring) => {
//           //   console.log('hello')
//           // })
//           ctx.hotstringManager.addHotStrings([
//             hotstring.create('hello', (ctx, removeInsertedHotstring) => {
//               console.log('hello')
//               removeInsertedHotstring()
//             }),
//           ])
//         },
//       },
//     },
//   ],
// })

// const host = document.getElementById('effitor-host') as HTMLDivElement
// editor.mount(host)

// window.ctx = editor.context
// // editor.bodyEl.innerHTML = `<et-p class="etp et"><span>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</span><span>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</span><span>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</span><span>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</span><span>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</span><span>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</span><span>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</span><span>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</span><span>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</span><span>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</span><b>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</b><b>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</b><b>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</b><b>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</b><b>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</b><b>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</b><b>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</b><b>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</b><b>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</b><b>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</b><span>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</span><span>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</span><span>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</span><span>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</span><span>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</span><span>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</span><span>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</span><span>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</span><span>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</span><span>区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上application 与 vendor</span></et-p>`

// // let i = 0
// // const addHeading = () => {
// //   const heading = document.createElement(EtHeading.elName) as EtHeading
// //   heading.headingLevel = (i % 6) + 1 as Et.HeadingLevel
// //   heading.textContent = `标题${heading.headingLevel}`
// //   i++
// //   editor.context.bodyEl.appendChild(heading)
// // }
// // const addparagraph = () => {
// //   const paragraph = document.createElement(EtParagraphElement.elName) as EtParagraphElement
// //   paragraph.textContent = `区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上a区分。这时候 hoist 的不现在 vendo和 application 的各上。因为 pn@6 B👨👩👧👦CAB👩👧👦C👨🏽👨🏽都建立在 pnpm@7 基础上a`
// //   editor.context.bodyEl.appendChild(paragraph)
// // }

// // const it = setInterval(() => {
// //   addHeading()
// //   Array.from({ length: 5 }).forEach(() => addparagraph())
// //   if (i > 100) {
// //     clearInterval(it)
// //   }
// // }, 0)

// editor.observeBody((ms) => {
//   for (const m of ms) {
//     console.log('record', m.type, m.addedNodes, m.removedNodes)
//   }
// }, {
//   childList: true,
//   subtree: true,
// })

// export {}
