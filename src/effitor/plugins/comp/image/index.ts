import { CompCode, type EtComponent } from "../@types.comp";
import { EtImageElement } from "./EtImageElement";

export const imageComp: EtComponent = {
    code: CompCode.IMAGE,
    element: EtImageElement,
}